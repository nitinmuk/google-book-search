import React from "react";
import Header from "../../components/Header";

const Saved = () => {
    return (
        <div>
            <Header/>
            Saved
        </div>

    );
}

export default Saved;